# n8n Workflows

Exported workflow JSON from n8n, for backup/version control.

**Note:** Any hardcoded API keys found in node parameters have been redacted
and replaced with `{{PLACEHOLDER}}` values. Re-add real credentials via
n8n's credential store (not hardcoded in node parameters) when re-importing.

## Workflows in this batch
- Jarvis Voice Adapter
- Jasmine's Jarvis (secret redacted: ElevenLabs API key)
- Jarvis Calendar Test (DO NOT USE IN PROD)
- Signl Cycle — Checkin Embedding Pipeline

More workflows will be added once MCP access is enabled on the remaining ones in n8n.
